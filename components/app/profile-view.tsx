'use client'

import { useState } from 'react'
import Image from 'next/image'
import Link from 'next/link'
import { ArrowLeft, BookOpen, Check, Flame, UserPlus } from 'lucide-react'
import { motion } from 'motion/react'
import { Button } from '@/components/ui/button'

export function ProfileView({ username }: { username: string }) {
  const [isFollowing, setIsFollowing] = useState(false)
  const displayName = username === 'bea-ramos' ? 'Bea Ramos' : username === 'carlo-santos' ? 'Carlo Santos' : 'Maria Dela Cruz'
  const pose = username === 'bea-ramos' ? '/mascot/coach-study.webp' : username === 'carlo-santos' ? '/mascot/coach-thumbs-up.webp' : '/mascot/coach-idea.webp'

  return <motion.main initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="mx-auto w-full max-w-[920px] px-4 pb-[calc(6rem+env(safe-area-inset-bottom))] pt-6 sm:px-7 sm:pt-9 lg:px-10 lg:pb-14"><Link href="/play" className="inline-flex min-h-10 items-center gap-2 rounded-[12px] border border-border bg-card px-3 text-xs font-extrabold text-muted-foreground transition-colors hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary"><ArrowLeft className="size-4" /> Back to Play</Link><section className="mt-5 overflow-hidden rounded-[24px] border border-border bg-card"><div className="h-24 bg-secondary sm:h-32" /><div className="relative px-5 pb-6 sm:px-8"><div className="-mt-12 flex flex-col gap-4 sm:-mt-16 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4"><div className="flex size-24 items-center justify-center rounded-[22px] border-4 border-card bg-[#eaf3ff] dark:bg-[#16294b] sm:size-32"><Image src={pose} alt="" width={128} height={128} className="size-full object-contain" /></div><div className="pb-1"><p className="text-xs font-extrabold uppercase tracking-[0.16em] text-primary">Fetch learner</p><h1 className="mt-1 font-display text-2xl font-extrabold tracking-[-0.04em] text-foreground sm:text-3xl">{displayName}</h1><p className="mt-1 text-sm text-muted-foreground">Biology, History, and a steady study rhythm</p></div></div><Button type="button" onClick={() => setIsFollowing((value) => !value)} variant={isFollowing ? 'outline' : 'default'} className="min-h-10 rounded-[12px] font-extrabold">{isFollowing ? <><Check className="size-4" /> Following</> : <><UserPlus className="size-4" /> Add friend</>}</Button></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Stat icon={Flame} label="Study rhythm" value="12 days" /><Stat icon={BookOpen} label="Cards recalled" value="428" /><Stat icon={Check} label="Recall quality" value="86%" /></div></div></section><section className="mt-5 grid gap-5 lg:grid-cols-[1fr_300px]"><div className="rounded-[22px] border border-border bg-card p-5 sm:p-6"><h2 className="font-display text-xl font-extrabold text-foreground">Recent study notes</h2><p className="mt-1 text-sm text-muted-foreground">Only activity this learner has chosen to share.</p><div className="mt-5 divide-y divide-border/70">{['Finished a 20-card Biology review', 'Opened a room for Cell biology sprint', 'Saved a difficult recall prompt'].map((item) => <div key={item} className="flex items-center gap-3 py-3"><span className="size-2 rounded-full bg-success" /><p className="text-sm font-semibold text-foreground">{item}</p><span className="ml-auto text-xs text-muted-foreground">Today</span></div>)}</div></div><aside className="rounded-[22px] border border-primary/15 bg-secondary/55 p-5"><p className="text-[11px] font-extrabold uppercase tracking-[0.16em] text-primary">Privacy first</p><h2 className="mt-2 text-lg font-extrabold text-foreground">Learning stays theirs.</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Profiles share study activity only when a learner chooses. Private decks, tutor chats, and due cards stay private.</p></aside></section></motion.main>
}

function Stat({ icon: Icon, label, value }: { icon: typeof Flame; label: string; value: string }) {
  return <div className="rounded-[15px] bg-secondary/65 p-4"><Icon className="size-4 text-primary" /><p className="mt-3 text-xs font-bold text-muted-foreground">{label}</p><p className="mt-1 text-lg font-extrabold tabular-nums text-foreground">{value}</p></div>
}
