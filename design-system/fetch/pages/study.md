# Fetch focused study override

Focused study is an immersive, deep-linkable route at `/decks/[id]/study`.

- Keep navigation minimal while preserving a safe, obvious exit.
- Make the prompt the visual anchor; the answer is revealed on demand.
- Use confidence controls with text labels and visible keyboard alternatives (`1–4`, `Space`, `Esc`).
- Preserve unfinished card index and ratings in session storage for the current deck.
- Reserve expressive color and mascot motion for a difficult retrieval or a completed due queue.
- Never rely on color alone to communicate correctness or progress.
